# Public deployment assets

TalonCV public production is static-hosted from `web/`. The active deployment guidance is [DEPLOYMENT.md](../DEPLOYMENT.md). There is no active Railway, Supabase, database, or Docker deployment configuration.

The Python files elsewhere in this repository remain local research/reference tooling.
